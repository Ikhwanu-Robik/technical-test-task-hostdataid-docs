---
title: Dummy Top Up API
description: A dummy top up API.
---

A backend API for a dummy top up system.

## Installation

1. Clone the project
```bash
git clone https://github.com/Ikhwanu-Robik/technical-test-task-hostdataid.git
cd technical-test-task-hostdataid
```

2. Install dependencies
```bash
composer install
```

3. Copy .env.example
```bash
cp .env.example .env
```

4. Generate application key
```bash
php artisan key:generate
```

5. Configure database
```dotenv title=".env"
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=technical-test-task-hostdataid
DB_USERNAME=root
DB_PASSWORD=
```

6. Start database server

7. Run migration and seeder
```bash
php artisan migrate --seed
```
or import the **public/technical_test_task_hostdataid.sql** in your database

8. Start php server
```bash
php artisan serve
```

## Testing

### with local URL
1. Download and open the **Postman Desktop App**.
2. Click the **Import** button in the top-left sidebar.
3. Paste this link: **https://www.postman.com/ikhwanu-robik/technical-test-hostdata-id/collection/m4cdrjd/technical-test-task-hostdata-id?action=share&source=copy-link&creator=43278751**.
4. Confirm the import. The collection will appear in your sidebar under "Collections."
5. Click on **Technical Test Task Hostdata.id**, a tab will open
6. Click on **Variables**
7. Change the **Current value** of the **host** variable to **127.0.0.1:8000**
8. Select a request to be tested
9. Change the body of the request to be tested
10. Run the selected request

### with live URL
1. Open this [Postman collection](https://www.postman.com/ikhwanu-robik/technical-test-hostdata-id/collection/m4cdrjd/technical-test-task-hostdata-id?action=share&source=copy-link&creator=43278751)
2. Sign in to your Postman account
3. Select a request to be tested
4. Change the body of the request to be tested
5. Run the selected request